Hi<%= @user.display_name ? " #{@user.display_name}" : "" %>,

Your sign-in code is: <%= @short_code %>

Enter this code in the browser where you requested it.

Or sign in from any browser by visiting:

<%= @url %>

This expires in 15 minutes.

If you didn't request this, you can safely ignore this email.
