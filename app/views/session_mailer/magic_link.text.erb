Hi<%= @user.display_name ? " #{@user.display_name}" : "" %>,

Sign in to Scriptures by visiting:

<%= @url %>

This link expires in 15 minutes.

If you didn't request this link, you can safely ignore this email.
